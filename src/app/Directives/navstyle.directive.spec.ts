import { NavstyleDirective } from './navstyle.directive';

describe('NavstyleDirective', () => {
  it('should create an instance', () => {
    const directive = new NavstyleDirective();
    expect(directive).toBeTruthy();
  });
});
